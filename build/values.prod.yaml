service:
  port: 80
ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/tls-acme: 'true'
  hosts:
    - host: docs.locklift.io
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: docs.locklift.io-tls
      hosts:
        - docs.locklift.io

livenessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10
